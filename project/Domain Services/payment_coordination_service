class PaymentCoordinationService {
  processBookingPayment(booking: Booking, paymentMethod: PaymentMethod): PaymentResult
  handlePaymentRefund(booking: Booking, refundAmount: Price): RefundResult
  reconcilePayments(dateRange: DateRange): ReconciliationReport
  handleFailedPayments(booking: Booking): RetryStrategy
}
